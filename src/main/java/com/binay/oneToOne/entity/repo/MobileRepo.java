package com.binay.oneToOne.entity.repo;

import com.binay.oneToOne.entity.Mobile;
import org.springframework.data.jpa.repository.JpaRepository;

public interface MobileRepo extends JpaRepository<Mobile, Long> {
}
